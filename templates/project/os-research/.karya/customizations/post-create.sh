#!/usr/bin/env bash
# Collection-level hook for project/os-research — shared setup across all OS research projects.
set -euo pipefail

echo "[os-research] post-create: project root = $PWD"
# Nothing here yet — project-specific hooks do the heavy lifting.
